# Local, machine-specific config (never commit real credentials)
/config/config.php

# Composer (dev-only) dependencies — the app itself has no runtime deps
/vendor/
composer.lock

# PHPUnit
/.phpunit.cache/
/.phpunit.result.cache

# OS / editor cruft
Thumbs.db
.DS_Store
